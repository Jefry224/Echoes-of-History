import { PageLayout } from '../components/layout';

export function HomePage() {
  return (
    <PageLayout title="Welcome">
      <section className="hero">
        <h2>React + TypeScript Starter</h2>
        <p>Your project is set up and ready to go.</p>
      </section>
    </PageLayout>
  );
}
