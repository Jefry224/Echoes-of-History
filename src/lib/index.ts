// ─── Re-exports (lib/index.ts) ────────────────────────────────────────────────
// Place third-party library wrappers or pre-configured instances here.
// Example:
//   export { QueryClient, QueryClientProvider } from '@tanstack/react-query';
//   export const queryClient = new QueryClient({ ... });

export {};
